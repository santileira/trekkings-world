import country from './country';
import region from './region';
import trek from './trek';
import socialPublication from './socialPublication';

export const schemaTypes = [country, region, trek, socialPublication];
